import CommonUriConstants from 'redpoints-model-js-common/constants/CommonUriConstants';
import BaseController from './BaseController';
import HttpMethods from '../enumeration/HttpMethods';
import HttpStatus from '../enumeration/HttpStatus';
import UrlBuilder from '../helper/UrlBuilder';

export default class asset extends BaseController {
  static async create(context, customerId, contractId, full, assetModel) {
    return BaseController.restApiCall(
      context,
      todo,
      pathVariables,
      queryParams,
      todo,
      todo,
      todo,
      body
    );
  }
  static async get(context, customerId, contractId, assetId, full) {
    return BaseController.restApiCall(
      context,
      todo,
      pathVariables,
      queryParams,
      todo,
      todo,
      todo,
      body
    );
  }
  static async update(
    context,
    customerId,
    contractId,
    assetId,
    full,
    assetModel
  ) {
    return BaseController.restApiCall(
      context,
      todo,
      pathVariables,
      queryParams,
      todo,
      todo,
      todo,
      body
    );
  }
  static async delete(context, customerId, contractId, assetId) {
    return BaseController.restApiCall(
      context,
      todo,
      pathVariables,
      queryParams,
      todo,
      todo,
      todo,
      body
    );
  }
  static async list(context, customerId, contractId, full, filters) {
    return BaseController.restApiCall(
      context,
      todo,
      pathVariables,
      queryParams,
      todo,
      todo,
      todo,
      body
    );
  }
  static async activate(context, customerId, contractId, assetId) {
    return BaseController.restApiCall(
      context,
      todo,
      pathVariables,
      queryParams,
      todo,
      todo,
      todo,
      body
    );
  }
  static async deactivate(context, customerId, contractId, assetId) {
    return BaseController.restApiCall(
      context,
      todo,
      pathVariables,
      queryParams,
      todo,
      todo,
      todo,
      body
    );
  }
  static async updateScopes(
    context,
    customerId,
    contractId,
    assetId,
    scopesModel
  ) {
    return BaseController.restApiCall(
      context,
      todo,
      pathVariables,
      queryParams,
      todo,
      todo,
      todo,
      body
    );
  }
  static async list(context, customerId, contractId, assetId, full) {
    return BaseController.restApiCall(
      context,
      todo,
      pathVariables,
      queryParams,
      todo,
      todo,
      todo,
      body
    );
  }
  static async mapAsset(full, entity) {
    return BaseController.restApiCall(
      context,
      todo,
      pathVariables,
      queryParams,
      todo,
      todo,
      todo,
      body
    );
  }
  static async mapAssets(full, assetEntities) {
    return BaseController.restApiCall(
      context,
      todo,
      pathVariables,
      queryParams,
      todo,
      todo,
      todo,
      body
    );
  }
}
