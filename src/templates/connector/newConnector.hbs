import CommonUriConstants from 'redpoints-model-js-common/constants/CommonUriConstants';
import BaseController from './BaseController';
import HttpMethods from '../enumeration/HttpMethods';
import HttpStatus from '../enumeration/HttpStatus';
import UrlBuilder from '../helper/UrlBuilder';


export default class {{connector_name}} extends BaseController {
{{#each connectorData}}
/**
{{#each this.methodParams}}
* @param {{this}}
{{/each}}
*/
static async {{this.methodName}}({{this.methodParams}})
{
const pathVariables = new Map();
const queryParams = new Map();




return BaseController.restApiCall(
context,
todo,
pathVariables,
queryParams,
HttpMethods,
HttpStatus,
todo,
body
)
}
{{/each}}
}